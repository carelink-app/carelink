# 요보망 — Play Console 테스터 배포 가이드 (v1.4.0)

## 📦 1단계: 새 AAB 빌드

`carelink\app-android\buildN.bat` 더블클릭

**결과물 위치:**
- `carelink\app-release.aab` ← 이걸 Play Console에 업로드
- `carelink\app-release.apk` ← 직접 설치용 (백업)

빌드 시간: 1~3분.
실패 시: cmd 창에 빨간 에러 메시지 → 복사해서 알려주세요.

---

## 🚀 2단계: Play Console 업로드

### 2-1. 접속
1. https://play.google.com/console 접속 → 요보망 앱 클릭
2. 좌측 메뉴 **테스트 → 내부 테스트** 클릭 (또는 비공개 테스트)

### 2-2. 새 출시 만들기
1. 우측 상단 **새 출시 만들기** 버튼
2. **App Bundle 추가** → `app-release.aab` 드래그 (또는 파일 선택)
3. 업로드 완료까지 30초~1분 기다리기

### 2-3. 출시 세부정보
- **출시 이름**: `1.4.0 (18)` (자동 채워짐)
- **출시 노트** 에 아래 텍스트 붙여넣기:

```
[변경사항]
• 사업자등록증 진위확인 시스템 (국세청 API 연동)
• 인증 기관 디렉토리 (지도/목록 + 길찾기)
• 지역 커뮤니티 (글쓰기/공감/댓글)
• 자격증/보수교육 분리 모드
• 카테고리 필터 강화 (방문/주간/시설)
• 신고 시스템 + 24시간 임시조치
• 차별 표현 자동 필터
• 새 앱 아이콘 (요보망)

[법적 보완]
• 개인정보 동의 6개 + 선택 2개로 분리
• 위치정보 별도 동의 (LBS법)
• 민감정보 별도 동의 (PIPA §23)
• 동의 기록 영구 보존
• 약관 버전 관리

[버그 수정]
• 하단 메뉴 시스템 버튼 정렬
• 글쓰기 뒤로가기 모달 정리
• 보수교육 신청 작동 문제
• 화면 코드 노출 버그
```

### 2-4. 출시 검토 → 출시
1. 우측 하단 **검토** 버튼
2. 경고가 있으면 해결 (보통 그대로 통과)
3. **내부 테스트 출시 시작**

업로드 후 5~30분이면 Google이 검토하고 테스터에게 배포됩니다.

---

## 👥 3단계: 테스터 알림

### Play Console에서 자동 알림 (권장)
1. **테스트 → 내부 테스트 → 테스터 탭**
2. 테스터 이메일 주소가 등록되어 있으면 → 자동으로 Google이 업데이트 가능 알림 발송

### 직접 안내 (확실한 방법)
**카카오톡/문자로 보낼 메시지 예시:**

```
🔔 요보망 1.4.0 업데이트 안내

새 기능이 많이 추가되었어요!

📱 업데이트 방법:
Play 스토어 → 요보망 → 업데이트
(테스트 트랙 자동 업데이트 켜져 있으면 자동)

🆕 주요 변경:
• 인증 기관 둘러보기 (지도)
• 지역 커뮤니티 (우리 동네 글쓰기)
• 사업자등록 진위확인 배지
• 자격증/보수교육 분리

링크: https://play.google.com/store/apps/details?id=com.yoyanglink.app

문의: ssw112300@gmail.com
```

### 테스터 추가 (필요 시)
**테스트 → 내부 테스트 → 테스터 탭 → 이메일 목록 만들기**
- 이메일 주소 한 줄에 하나씩 입력
- 테스터에게 옵트인 링크 공유 → Google 계정으로 가입

---

## ⚠️ 트러블슈팅

### "AAB 업로드 실패: versionCode 충돌"
현재 18번. 이전에 18을 사용했다면 → `build.gradle`에서 `versionCode 19` 로 더 올린 후 재빌드.

### "서명 인증서 불일치"
첫 출시 시 사용한 키스토어와 다르면 거부됨. `app-android\yoyanglink-keystore.jks` 가 그대로 있어야 함. (Google Play App Signing 사용 중이면 자동 처리)

### "출시 검토 거부"
- 권한 사용 사유 명시 (위치, 카메라 등)
- 콘텐츠 등급 다시 확인
- 개인정보처리방침 URL: https://carelink-app.github.io/carelink/#privacy

### "업데이트 알림 안 옴"
- Play 스토어에서 **자동 업데이트 → 켜기** 안내
- 또는 직접 검색: "요보망"

---

## 📋 출시 후 확인

1. https://play.google.com/store/apps/details?id=com.yoyanglink.app — 버전 1.4.0 표시 확인
2. 본인 핸드폰에서 Play 스토어 → 요보망 → 업데이트 (자동 못 받으면 수동)
3. 업데이트 후 앱 실행 → 새 기능 작동 확인:
   - 회원가입 동의 8개 항목 체크
   - 홈 화면 카테고리 칩 (전체/방문/주간/시설)
   - 커뮤니티 메뉴 (지역별)
   - 사업자 인증 모달 (기관 회원만)
   - 신고 버튼

---

## 🔄 다음 빌드 시

`build.gradle` versionCode를 19, 20... 식으로 매번 +1 올려야 합니다 (Play Store 규칙).

versionName(1.4.0)은 의미별로 본인이 정함:
- 마이너 버그 수정: 1.4.1
- 작은 기능 추가: 1.5.0
- 큰 변화: 2.0.0
