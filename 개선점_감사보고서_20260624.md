# 요보망 개선점·불편사항 종합 감사 보고서

작성일: 2026-06-24 · 대상: `index.html`(13,122줄) + Firebase Rules / Functions / Cloudflare Worker / 정책 페이지
방식: 기능·보안·성능·접근성/법무/운영 4개 영역 정밀 코드 감사 (모든 항목 실제 줄번호 근거)

---

## 한눈에 보기

총 **57개 보완점** 발견. 우선순위별 분포:

- 🔴 **P0 (즉시)**: 11건 — 보안 권한 부재 3, 에러 안내 누락 4, 운영 인프라 3, 접근성 1
- 🟡 **P1 (완성도)**: 24건
- 🟢 **P2 (디테일)**: 22건

**가장 시급한 3가지**
1. Firebase Rules가 루트 `.read/.write: true` — 누구나 전 회원 개인정보·채팅을 덤프하고 데이터를 파괴할 수 있는 상태. (보안 #1)
2. Firebase 실시간 리스너 3곳에 에러 콜백 누락 — 네트워크 끊김 시 어르신 화면에 스피너/빈화면이 영구히 남음. (기능 #2·#3·#4)
3. 동의는 받아놓고 미제공 중인 기능들 — 푸시 알림(FCM)·행동분석이 코드에 없는데 가입 시 동의를 받고 개인정보처리방침에 명시. 정책-구현 불일치. (운영 #2·#3)

---

## 🔴 P0 — 즉시 처리

### 보안 (서버측 권한 부재가 핵심)

| # | 위치 | 문제 | 공격 시나리오 | 개선안 |
|---|------|------|---------------|--------|
| S1 | `database.rules.json:5-6` | 루트 `.read:true / .write:true` | `databaseURL/.json` 으로 전 회원 PII(전화·사업자번호·생년)·전체 채팅 덤프, 임의 노드 삭제로 모든 공고·채팅 파괴 | 카카오 토큰→Cloud Function→Firebase Custom Token 발급으로 `auth.uid` 부여 후 노드별 `auth != null` + 소유자 규칙 적용 |
| S2 | `index.html:11981-12046` | 차단(밴) 100% 클라이언트 검증 + rules 무방비 | 공격자가 자기 밴 기록 삭제 후 재로그인, 또는 운영자 ID를 밴 등록해 서비스 거부. 콘솔에서 `state.bannedUsers={}` 한 줄로 우회 | 밴 enforcement를 서버 규칙/Function으로 이전 |
| S3 | `index.html:8749, 10150` | 관리자 권한이 클라 ID 비교(`kakaoId===ADMIN_KAKAO_ID`)뿐 | 콘솔에서 `state.user.kakaoId='4818030525'` 설정 시 관리자 대시보드·전 회원·밴/삭제 전부 활성화 | Custom Claims(`admin:true`) 기반 규칙으로 보호 |

> S1 하나를 해결하면 S2·S7·S13이 동시에 해소됩니다. 전체 보안의 약 80%.

**별도 긴급**: `firebase-functions/index.js:16` 배포 주석에 국세청 인증키 일부가 평문 노출 → **키 즉시 재발급** 권장.

### 기능 — 에러 안내 누락 (어르신이 가장 막막해하는 지점)

| # | 위치 | 문제 | 개선안 |
|---|------|------|--------|
| F1 | `index.html:7647` `loadMessages` | 채팅 실시간 리스너에 에러 콜백 없음 → 끊김 시 메시지 영영 안 뜸 | `.on('value', fn, errFn)` 추가 → "메시지를 불러올 수 없어요. 인터넷 확인 후 다시 들어와 주세요" |
| F2 | `index.html:10907` `loadCommunityPosts` | 로드 실패 시 "⏳ 불러오는 중…" 스피너가 영구히 남음 | 에러 콜백 + 재시도 버튼 |
| F3 | `index.html:7315` `loadListingsViaRest` | 공고 로드 실패 시 `console.error`만 — 0건인지 실패인지 구분 불가 | catch에서 "공고를 불러오지 못했어요. 다시 시도" 버튼 |
| F4 | `index.html:5618` `confirmMatch` | 매칭완료 누르면 즉시 비가역 처리 + 상대에게 상세주소 전송. "취소 불가" 명시 없음 | 모달에 "한 번 완료하면 되돌릴 수 없어요" 굵게 + 상대 이름 표시 후 확인 |

### 운영·접근성 인프라

| # | 위치 | 문제 | 개선안 |
|---|------|------|--------|
| O1 | 전역 | 에러 모니터링(Sentry/Crashlytics) 완전 부재 + 빈 `catch(e){}` 다수 → 운영자가 프로덕션 오류 인지 불가 | Sentry/Firebase Crashlytics 통합 |
| O2 | `index.html:3895, 8308` | FCM 미통합. 알림은 포그라운드 listener만 → 앱 종료 시 푸시 불가. 그런데 가입 시 푸시 동의(667줄) 수령 → **동의받고 미제공** | `firebase-messaging` + SW 통합 |
| O3 | 전역 | Analytics 미설정. 개인정보처리방침 §2에 "이용 통계·분석" 명시했으나 수집 코드 없음 → 정책-구현 불일치 | GA4/Firebase Analytics 추가 또는 정책 문구 삭제 |
| A1 | 전역 (aria-label 3개뿐) | `role="dialog"`/`aria-modal` 0개, `aria-live` 0개. 30+ 모달·토스트가 스크린리더 미노출 | 모달에 `role="dialog" aria-modal aria-labelledby`, 토스트에 `aria-live="polite"` |

---

## 🟡 P1 — 완성도 향상

### 기능·UX

| # | 위치 | 문제 | 개선안 |
|---|------|------|--------|
| F5 | `index.html:6816, 4363` | 글쓰기 중 탭 이동 시 작성 내용 전부 소실. 임시저장·경고 없음 | 입력값 있을 때 이탈 confirm 또는 localStorage 임시저장 |
| F6 | `index.html:1270, 8447` | 채팅방 헤더에 상대 이름만 — 역할/인증기관 배지 없음 → 사칭·오해 위험 | 헤더에 역할 배지(🏥기관/👩‍⚕️보호사) + ✓인증 표시 |
| F7 | `index.html:5485` `renderListings` | 검색 0건 시 어떤 필터 때문인지 안 보이고 초기화 버튼 없음 | "필터 초기화"/"전체 보기" 버튼 + 적용 필터명 표시 |
| F8 | `index.html:1346` 채팅 입력바 | 키보드 올라올 때 입력창/메시지 가림 보정 없음(`visualViewport` 미사용) | `visualViewport` resize 보정 + focus 시 마지막 메시지로 스크롤 |
| F9 | `index.html:5559, 5743` | "매칭 후 실번호 공개"라 안내하지만 `tel:` 링크는 즉시 실번호로 연결 → 문구-동작 불일치 | 문구를 동작에 맞게 통일 또는 정책 확정 |

### 보안

| # | 위치 | 문제 | 개선안 |
|---|------|------|--------|
| S4 | `firebase-functions/index.js:137-245` | verifyBusiness 인증·rate limit 부재 → 국세청 API 무한 호출/쿼터 소진, 사업자번호 enumeration | App Check/ID토큰 검증 + IP·유저별 rate limit + b_no 캐싱 |
| S5 | `index.html:10706` | 클라가 verifyBusiness 직접 POST, 토큰 미첨부. 응답 `raw`에 국세청 원본 노출 | ID 토큰 첨부 + 응답에서 `raw` 제거 |
| S6 | `index.html:2835-2876` | 카카오 토큰을 하드코딩 XOR 키로 localStorage 저장(소스에 키 평문) → XSS/기기 접근 시 토큰 탈취 | 네이티브 KeyStore/HttpOnly 쿠키 저장, 웹 저장 만료 단축, 키 빌드타임 주입 |
| S7 | `index.html:7231-7257` | databaseURL 노출 + 비인증 REST(`fbRestGet/Set`) → 전 회원 PII GET, 임의 PUT | S1 규칙 적용 시 자동 해소 + REST도 `auth` 파라미터 요구 |
| S8 | `rules.json:38`, `index.html:7828` | 채팅 imageUrl `data:image/` 허용 → SVG data URI에 `<script>` 삽입, 클릭 시 실행/피싱 | `data:` 스킴 차단(https만), SVG data URI 금지 |

### 성능·메모리

| # | 위치 | 문제 | 개선안 |
|---|------|------|--------|
| P1 | `index.html:8327/8362/6941` | `chats/{id}/meta` 리스너가 경로 불일치로 detach 안 됨 → 재로그인 시 누적·중복 알림음 | meta 핸들러 참조 저장 후 `off('value',handler)` |
| P2 | `index.html:8367` | `chats` child_added 전체 노드 구독(방 전체 메시지 포함 다운로드) | 본인 참여방 인덱스(`user-chats/{uid}`) 구독으로 전환 |
| P3 | `index.html:7373` | `listings` 전체 노드 구독(limitToLast 없음) | `orderByChild('createdAt').limitToLast(200)` |
| P4 | `index.html:8910` | `edu-regions` value 리스너 `.off()` 전무 → 재로그인 누수 | 핸들러 보관 후 로그아웃 시 off |
| P5 | `index.html:12010` | `banned_users` value 리스너 `.off()` 없음 → 중복 등록 누수 | 등록 전 off 또는 1회 가드 |
| P6 | `index.html:5487` `renderListings` | 수백~수천 카드를 forEach→innerHTML 일괄 대입(가상스크롤 없음) → 저사양 WebView 프리즈 | 가상 스크롤 또는 limitToLast 연계 렌더 상한 |
| P7 | `index.html:7831, 12934` `<img>` | `loading="lazy"` 0건 → 채팅/스티커 진입 시 전 이미지 동시 로드 | 모든 `<img>`에 `loading="lazy" decoding="async"` |

### 접근성·법무

| # | 위치 | 문제 | 개선안 |
|---|------|------|--------|
| A2 | `index.html:676` | "카카오 로그인 시 필수 약관에 자동 동의됩니다" 문구가 실제 코드(체크박스 차단)와 모순 → PIPA §22 오해 소지 | "필수 약관 동의 후 카카오로 로그인됩니다"로 수정 |
| A3 | `index.html:854, 2079` | 공지 배너·모달 마케팅 발송 시 '(광고)' 라벨 누락(정통망법 §50④) | 마케팅 동의자 대상 발송 시 "(광고)" 자동 삽입 |
| A4 | `index.html:1352` | 채팅 전송 버튼 `➤` aria-label 없음 | `aria-label="메시지 전송"` |
| A5 | `index.html:2128` 등 모달 닫기 | `<span onclick>✕` — 키보드 포커스 불가, aria-label 없음 | `<button>` + `aria-label="닫기"` |
| A6 | `index.html:380, 424` | 터치영역 `min-height:44px`만, min-width 없음 → 소형 버튼 폭 미달(WCAG 2.5.5) | 아이콘 버튼에 `min-width:44px` |
| A7 | `index.html:205, 225, 246` | 회색 텍스트 대비 부족(#999/#aaa, 4.5:1 미달) → 어르신 가독성 | 본문 회색 `#595959` 이상으로 |
| A8 | `index.html:146-171` | fs-l/fs-xl가 클래스만 재정의, 인라인 `style="font-size"`는 미적용 → 글자크기 키워도 안 커지는 텍스트 | 인라인 font-size 제거·클래스화 또는 rem 스케일 |
| A9 | `index.html:424` | 전역 `[onclick]{min-height:44px}`가 인라인 텍스트 링크까지 44px 강제 → 줄간격 깨짐 | 버튼류 클래스로 한정 |

---

## 🟢 P2 — 디테일 (체감 개선)

### 기능·UX
- **공고 카드에 근무 요일/시간 미표시**(`5487`) → 카드에 `📅 ${days}` 한 줄 추가. 매번 상세 진입하는 수고 제거.
- **교육신청 버튼 더블탭 방어 없음**(`9630`) → 진입 시 `disabled` + "신청 중…" 표시.
- **채팅 목록 미리보기**(`8419`) → 서류/스티커 전송 시 타입별 라벨("📋 자격증 보냄").
- **빈 채팅 화면에 공고 이동 버튼 없음**(`1255`) → "공고 보러 가기 →" 버튼(내 공고 빈상태엔 이미 있음, 일관성).
- **파일 크기 오류만 `alert`**(`6967`) → 나머지처럼 `showToast`로 통일.
- **채팅 전송 실패 재시도 불가**(`7975`) → 실패 메시지에 "⚠️ 다시 보내기" 인라인 액션.

### 보안
- **`processMessageTextForLinks` lat/lng를 onclick 직접 보간**(`7931`) → `Number()` 강제 변환·범위 검증.
- **신고 렌더링 escape 누락**(`10380`) → 관리자가 신고함 열 때 XSS(권한 상승). 모든 report 필드 escape.
- **onclick 속성에 listingId/title escape·quote 없이 보간**(`10384, 10466`) → data-속성+이벤트 위임으로.
- **Cloudflare Worker Origin이 substring 매칭**(`worker.js:48`) → `evil` 우회 가능. allowlist `===` 비교.
- **OAuth에 state 파라미터 없음**(`4747`) → CSRF 방어용 state 추가 + 카카오 콘솔 Redirect URI 점검.

### 성능
- **외부 라이브러리 7개 동기 차단 로드**(`19~31`, xlsx ~900KB) → 비필수(xlsx·leaflet) `defer`/동적 import.
- **devtools 감지 `setInterval(1000)` 무한 + clearInterval 없음**(`3185`) → 매초 강제 레이아웃 4회. 주기 완화/제거.
- **REST 폴링 15초마다 전체 listings GET**(`7297`) → 쿼리 파라미터로 limit 적용.
- **채팅/커뮤니티/내공고 렌더 모두 forEach→innerHTML**(`8045, 10929, 10481`) → 부분 갱신/렌더 디바운스.

### 법무·접근성
- **광고 라벨 "광고"(괄호X) + font-size 9px**(`10126`) → "(광고)" + 11px↑.
- **자동수집 로그 보유기간 미명시**(`privacy-policy.html:48`) → 항목별 보유기간(예: 접속로그 3개월) 명시.
- **14세 미만 차단이 자기신고 체크박스만**(`644`) → 카카오 생년월일 연동 서버 검증 권장.
- **정책 페이지 장식 이모지 스크린리더 노출**(`data-deletion.html`) → `aria-hidden="true"`.

---

## ⚡ 즉시 적용 Quick Wins (각 5~10분)
1. 파일 크기 오류 `alert` → `showToast` 통일 (F #14)
2. 채팅 전송 버튼 `aria-label="메시지 전송"` (A4)
3. 로그인 "자동 동의" 모순 문구 수정 (A2)
4. 공고 카드에 근무 요일 한 줄 추가 (P2 기능)
5. 모든 `<img>`에 `loading="lazy"` 추가 (P7)
6. 광고 라벨 "(광고)" + 글자 크기 상향 (P2 법무)

## 잘 되어 있는 부분 (참고)
- 공고 등록폼: 중복제출 방어(`window._isSubmittingPost`)·차별표현 필터·콘텐츠 검증 탄탄
- 사용자 콘텐츠 렌더링 대부분 `escapeHtml/sanitizeInput` 일관 적용
- 약관 본문: 책임한도·분쟁해결(제9조)·관할법원·통신판매중개자 면책(제8조)·청소년보호정책 모두 존재
- `logConsent` 동의기록(분쟁 증거)·메시지 rate limit·localStorage quota 처리 구현됨
- 채팅 messages·seat·community 리스너는 올바르게 `.off()` 처리됨

## 권장 처리 순서
1. **S1 Firebase Custom Token + Rules 잠금** (S2·S7·S13 동시 해소, 보안 80%)
2. **F1~F3 리스너 에러 콜백** + **F4 매칭 경고** (어르신 체감 큼, 각 20분)
3. **O1~O3 운영 인프라**(에러 모니터링·FCM·Analytics) — 정책 일치
4. **Quick Wins 6종** 일괄 처리
5. P1 성능 리스너 누수(P1~P7) → P2 디테일
